# Firestone

Follow `AGENTS.md`. Read `SPEC.md` and `dev/PROJECT_STATUS.md` before making changes.

`dev/` is local-only and untracked. It holds the orchestrator's working state, so nothing in it belongs in a pull request.

