# virtiofsd distribution verification

This record covers the Firestone-owned virtiofsd v1.14.0 build recipe. The first pull request establishes reproducible candidate binaries. Publication and `deps.toml` binary pins follow only after review and merge.

## Pinned inputs

`build/virtiofsd/versions.env` holds every trust input consumed by the recipe.

| Input | Pin |
|---|---|
| Upstream source | commit `c2540f8db14caba81c1e37fba23fc7bf2cd7f0dd`; archive SHA-256 `52b66e449ca583b4f050a2bff327ff812211a2c349b4130279fcfc6a64540f04` |
| Cargo lockfile | SHA-256 `995b73803681900135edc1e47127fec62832ec7503c87c4eb1f3b18dc5e1d866` |
| Rust | 1.98.0, rustc commit `88d9e12ae178fab0fb5cc050a94da85685d449ea`, Cargo commit `797e8a9bca276c1c9f9f738d2a20f484fa4eea9d` |
| aarch64 Rust standard library | 2026-08-20 release archive SHA-256 `4367ba0e6b8fa5ef9c195d12f0ab4a39b8f08cda627cf121f5f929d47161f4b4` |
| Rust builder | `rust@sha256:a10e64dd139b7387337c7fbe8aca31b959b57b2fd4c8ae20a02cf1d6ea424dce` |
| Cross-build Rust host | `rust@sha256:4e4a7e7939c17991ab35f2b8c2e67593980f771d28f6b1254b1850f860fd0c7f` |
| aarch64 cross builder | `ghcr.io/cross-rs/aarch64-unknown-linux-musl@sha256:f604e399cbb2154ddeb013db99eb4f123d24f09a579c7e8d6ed631d15ffa8b12` |
| Native libraries | Alpine 3.22 `libcap-ng-static` 0.8.5-r0 and `libseccomp-static` 2.6.0-r0; every architecture-specific APK has a checked SHA-256 |
| x86_64 tools | GCC 15.2.0, binutils 2.45.1, musl 1.2.6, pinned by the Rust builder digest |
| aarch64 tools | GCC 9.2.0, binutils 2.33.1, musl 1.2.3; compiler, wrapper, libc archive, and containing image are hash-pinned |
| Build epoch | `SOURCE_DATE_EPOCH=1783331602`, the upstream commit time `2026-07-06T09:53:22Z` |

The outer script accepts only HTTPS downloads and checks the source and native-library hashes before starting a container. Cargo uses the committed lockfile with `--locked`; Cargo verifies registry package checksums recorded in that file. The container runs without capabilities, with a read-only root filesystem, and can write only to its temporary build mount and the caller's explicit output directory.

Output publication takes an atomic per-artifact directory lock. The publisher rejects every existing binary, checksum, or build-info path, including dangling symlinks. It constructs the complete triplet in a private randomized directory with modes 0755, 0644, and 0644, then uses exclusive no-following renames. The two sidecars move first and the binary moves last. Cleanup is limited to validated lock and staging paths.

## Build flags

The release build uses upstream's static-link requirements and fixes inputs that can affect generated bytes:

```text
cargo build --locked --release --target <target>
RUSTFLAGS=-C target-feature=+crt-static -C link-self-contained=yes -C debuginfo=0 -C codegen-units=1 -C metadata=firestone-virtiofsd-v1.14.0 -C link-arg=-Wl,--build-id=none --remap-path-prefix=/work=/firestone-build --remap-path-prefix=/usr/local/cargo=/rust-cargo --remap-path-prefix=/usr/local/rustup=/rust-toolchain
CARGO_INCREMENTAL=0
CARGO_PROFILE_RELEASE_CODEGEN_UNITS=1
CARGO_PROFILE_RELEASE_DEBUG=0
CARGO_PROFILE_RELEASE_LTO=true
LIBCAPNG_LINK_TYPE=static
LIBSECCOMP_LINK_TYPE=static
SOURCE_DATE_EPOCH=1783331602
TZ=UTC
```

The recipe runs the target strip tool after linking. It then checks the ELF machine, rejects `PT_INTERP` and dynamic `NEEDED` entries, requires mode 0755, and runs the x86_64 binary with `--version`.

## Reproduction commands

Run each target twice into four empty directories outside the worktree:

```sh
scripts/build-virtiofsd.sh --arch x86_64 --output-dir /tmp/virtiofsd-x86-run-1
scripts/build-virtiofsd.sh --arch x86_64 --output-dir /tmp/virtiofsd-x86-run-2
scripts/build-virtiofsd.sh --arch aarch64 --output-dir /tmp/virtiofsd-arm-run-1
scripts/build-virtiofsd.sh --arch aarch64 --output-dir /tmp/virtiofsd-arm-run-2
```

The GitHub workflow performs the same four clean builds and compares each target byte for byte. It creates one deterministic GNU tar per target with sorted names, numeric owner/group 0, the pinned source epoch on every member, binary mode 0755, and sidecar mode 0644. The workflow extracts and checks the tar before uploading it for 14 days with `archive: false`. It never publishes a release.

## Linux results

The final build used bare-metal host `w`, an x86_64 Ubuntu machine running Docker 29.7.2 and buildx 0.36.1. The checked-out recipe was rebased onto `origin/main` commit `c49f39b233609e6131336d4a7829861c5a402561` before these commands ran.

```sh
scripts/build-virtiofsd.sh --arch x86_64 --output-dir /tmp/firestone-virtiofsd-corrected.QaAURS/x86-run-1
scripts/build-virtiofsd.sh --arch x86_64 --output-dir /tmp/firestone-virtiofsd-corrected.QaAURS/x86-run-2
scripts/build-virtiofsd.sh --arch aarch64 --output-dir /tmp/firestone-virtiofsd-corrected.QaAURS/arm-run-1
scripts/build-virtiofsd.sh --arch aarch64 --output-dir /tmp/firestone-virtiofsd-corrected.QaAURS/arm-run-2
```

Each invocation downloaded and checked its own source and native library archives. The aarch64 invocations also downloaded and checked the pinned Rust target library. Every run used a new Cargo home, target directory, and source extraction directory. Docker reused the reviewed builder layers, but no Cargo or source build state crossed between runs.

| Target | Run | Cargo compile time | Size | SHA-256 |
|---|---:|---:|---:|---|
| `x86_64-unknown-linux-musl` | 1 | 37.76 s | 2,728,944 bytes | `9ad3e33c45dd816b24ad483b60ca469974ba54c3b37ef93be3da2a623986646f` |
| `x86_64-unknown-linux-musl` | 2 | 36.86 s | 2,728,944 bytes | `9ad3e33c45dd816b24ad483b60ca469974ba54c3b37ef93be3da2a623986646f` |
| `aarch64-unknown-linux-musl` | 1 | 22.68 s | 2,355,632 bytes | `e45bd62e346eca87857279d5680782e80148379fbca524a648089f642ac001d2` |
| `aarch64-unknown-linux-musl` | 2 | 22.68 s | 2,355,632 bytes | `e45bd62e346eca87857279d5680782e80148379fbca524a648089f642ac001d2` |

`cmp` reported no difference between the two binaries or the two build-info files for either target. Independent host checks produced this evidence:

```text
virtiofsd-v1.14.0-x86_64-unknown-linux-musl:  ELF 64-bit LSB pie executable, x86-64, static-pie linked, stripped
  Type: DYN (Position-Independent Executable file)
  Machine: Advanced Micro Devices X86-64
  mode=755 PT_INTERP=absent NEEDED=absent build_paths=absent
  --version: virtiofsd 1.14.0

virtiofsd-v1.14.0-aarch64-unknown-linux-musl: ELF 64-bit LSB executable, ARM aarch64, statically linked, stripped
  Type: EXEC (Executable file)
  Machine: AArch64
  mode=755 PT_INTERP=absent NEEDED=absent build_paths=absent
```

The mode-preserving packages and full logs are on `w` under:

```text
/tmp/firestone-virtiofsd-corrected.QaAURS/release-candidates/
/tmp/firestone-virtiofsd-corrected.QaAURS/logs/
```

| Package | SHA-256 |
|---|---|
| `virtiofsd-v1.14.0-x86_64-unknown-linux-musl.tar` | `014d575701c5ecce57b31ff30ddcea684bc8db2952d82c9f22983ecf8816aa2a` |
| `virtiofsd-v1.14.0-aarch64-unknown-linux-musl.tar` | `cdf5e8015286e5700b019dcecb1059e7eb9f15b93377506c91b9190923a94382` |

Both tars contain exactly three sorted entries: `SHA256SUMS`, the binary, and its build-info file. Independent extraction produced binary mode 0755, sidecar mode 0644, numeric owner/group 0, and member time `2026-07-06T09:53:22Z`. The extracted checksums pass and both binaries match the bare-metal build outputs byte for byte.

The aarch64 result has compile-time and ELF verification only. Running it requires an aarch64 Linux host and remains part of the M5 native release matrix.

## Validation

The final recipe passed these checks:

- ShellCheck from `koalaman/shellcheck@sha256:bb596a0d169b85ddd81d8b6d3a2ff6d5baf5fca10b97f575ebc647c3dff62b3d` with external sources enabled.
- Hadolint 2.15.1 and Actionlint 1.7.12 with no findings.
- `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` on macOS. The test suite reported 47 passing tests.
- The same Rust gate on `firestone@172.203.242.136` with Rust 1.98.0. The Linux suite reported 48 passing tests, and `/dev/kvm` was readable and writable.
- Controlled failures for an unsupported architecture, an output path inside the git worktree, a non-HTTPS source URL, and a deliberately wrong source SHA-256. All four stopped before Docker compiled code. No `firestone-virtiofsd-build.*` temporary directories remained afterward.
- A controlled concurrent-output test starts two publishers with different binary and build-info bytes. Exactly one succeeds, and its three published files come from the same contender.
- A preexisting build-info symlink is rejected without replacing the link or modifying its target. The failure leaves no binary, checksum, lock, or stage path.
- The package test creates the same tar twice and requires byte identity. It then checks member names, modes, numeric ownership, fixed timestamps, extraction, and `SHA256SUMS`.

## Publication boundary

The candidate binaries are not dependency pins. After the orchestrator reviews and merges this recipe, the orchestrator will publish both files as Firestone prerelease assets. A follow-up pull request will download those immutable URLs, verify their hashes, change `deps.toml` from `source-only` to `binary`, and update `scripts/pin-deps.sh`. Until that work lands, `doctor --fix` must continue to report the virtiofsd distribution gap.
