# Project status

This file is the durable handoff for Firestone development. The orchestrator updates it on `main` after every merge wave. Agents update only their assigned row when a pull request changes its scope or status.

## Current position

- Milestone: Linux x86_64 MVP accepted; M0 through M5 are complete. M6 "daily driver" (v0.2.0) is in progress: snapshots, clone, resize, metrics, cp, prune, cloud-init auth, OCI image boot, and web UI parity (terminal, editing, sparklines). Frozen interface contracts and the wave plan live in SPEC §22 M6 and the M6 work-queue rows below.
- Baseline: exact clean main `47daf52` passed E2E 1–11 and resolved verify 1–17 on real x86_64 KVM; PTY UX, REST/serve, five-image catalog, doctor matrix, user guide, completions, and reproducible x86_64 release artifacts are merged
- Linux validation host: bare-metal `w`, Linux 6.8.0-137-generic x86_64, `/dev/kvm` read/write; temporary acceptance setup was removed and host policy restored
- Required local gate: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`
- Required integration gate: the same commands plus `scripts/m5-linux-mvp-e2e.py` on a clean Linux x86_64 KVM host
- Final acceptance: SHA-256 `e29a3fc7d189e29ea7a789783ce585ee4247032aa5be000931964107fd7faff4`; exact-main CI `33335967366`, doctor matrix `33335967251`, and release `33335967353` passed

## Work queue

| ID | Work | Branch | PR | Status | Depends on | Acceptance |
|---|---|---|---|---|---|---|
| M0-00 | Workspace, crate boundaries, typed errors, events, actions, dispatcher seam, CI | `agent/m0-foundation` | [#1](https://github.com/0xchasercat/firestone/pull/1) | complete | baseline | Workspace builds; architecture boundaries compile; CI runs the required gate |
| M0-01 | Paths, spec and patch model, layering, validation, drift coverage | `agent/m0-spec` | [#6](https://github.com/0xchasercat/firestone/pull/6) | complete | M0-00 | SPEC sections 5 through 7 unit coverage passes |
| M0-02 | Atomic files, machine state, locking, liveness reconciliation | `agent/m0-state` | [#4](https://github.com/0xchasercat/firestone/pull/4) | complete | M0-00 | Reconcile matrix and lock contention tests pass |
| M0-03a | Built-in catalog data and authoritative source validation | `agent/m0-catalog-data` | [#2](https://github.com/0xchasercat/firestone/pull/2) | complete | baseline | Initial entries have current per-architecture URLs and checksum sources; unbooted entries are marked as release gates |
| M0-03b | Image reference parsing, catalog merge, architecture selection, resolution | `agent/m0-catalog` | [#5](https://github.com/0xchasercat/firestone/pull/5) | complete | M0-00, M0-03a | Catalog rules from sections 8.1 and 8.2 pass without network |
| M0-04 | CLI parser, renderers, create/list/show/edit/doctor, dispatcher adapters | `agent/m0-cli` | [#9](https://github.com/0xchasercat/firestone/pull/9) | complete | M0-01, M0-02, M0-03b, M0-05a, M0-05b | M0 no-KVM command acceptance and renderer snapshots pass |
| M0-05a | Dependency manifest, real pins, download/hash tooling, third-party evidence | `agent/m0-deps` | [#3](https://github.com/0xchasercat/firestone/pull/3) | complete | baseline | Real per-architecture URLs and checksums; verified behavior recorded against exact versions |
| M0-05b | Host checks, doctor report, unprivileged fixes, deterministic tests | `agent/m0-doctor` | [#8](https://github.com/0xchasercat/firestone/pull/8) | complete | M0-01, M0-02, M0-05a | Section 17.3 checks and safe fixes pass without KVM |
| M0-05c | Reproducible virtiofsd builds and Firestone-owned release assets | `agent/m0-virtiofsd-pins` | [#7](https://github.com/0xchasercat/firestone/pull/7) + [#10](https://github.com/0xchasercat/firestone/pull/10) | complete | M0-05a | Both musl targets build from pinned inputs; published assets have verified immutable URLs and checksums in `deps.toml` |
| M0-06 | M0 integration, docs, Linux verification | `main` | [#8](https://github.com/0xchasercat/firestone/pull/8) + [#9](https://github.com/0xchasercat/firestone/pull/9) + [#10](https://github.com/0xchasercat/firestone/pull/10) | complete | M0-01 through M0-05c | M0 acceptance is green locally, in CI, and on Linux |
| M1-01 | Image store, pull/verify, raw conversion, overlay, remove, prune | `agent/m1-images` | [#13](https://github.com/0xchasercat/firestone/pull/13) | complete | M0 | Immutable source identity, owned qcow2 storage, overlay and image lifecycle tests pass |
| M1-02 | Deterministic cloud-init seed and canonical typed VmConfig | `agent/m1-seed-vmconfig` | [#12](https://github.com/0xchasercat/firestone/pull/12) | complete | M0 | CIDATA golden and every VmConfig mapping/overlay contract pass without KVM |
| M1-03 | Bounded Cloud Hypervisor Unix-socket API client | `agent/m1-vmm-api` | [#11](https://github.com/0xchasercat/firestone/pull/11) | complete | M0 | Exact v53 methods, status codes, framing limits, and error bodies are covered against a fake server |
| M1-04 | Shim process supervision, launch, status, stop, and cleanup | `agent/m1-shim` | [#14](https://github.com/0xchasercat/firestone/pull/14) | complete | M1-01, M1-02, M1-03 | Linux x86_64 acceptance uses pinned Cloud Hypervisor v53 + edk2; ordered launch/stop, ownership cleanup, deadlines, lock lifetime, client detachment, child reaping, and verified crash recovery pass. aarch64 runtime, non-Linux authority, and hostile wrapper containment are deferred |
| M1-05 | Lifecycle and image CLI integration | `agent/m1-lifecycle` | [#15](https://github.com/0xchasercat/firestone/pull/15) | complete | M1-01 through M1-04 | Linux x86_64 fake-VMM lifecycle covers shared start/stop/restart/rm/images/logs/show-vmconfig actions, deterministic rendering, bounded secure logs, safe deletion, and supervised/unsupervised stop; real KVM evidence remains M1-06 |
| M1-06 | M1 KVM integration and verify evidence | `agent/m1-integration` | [#16](https://github.com/0xchasercat/firestone/pull/16) | complete | M1-01 through M1-05 | Linux x86_64 E2E 1, 5, 6, and 7 pass under pinned Cloud Hypervisor v53 + edk2; serial login and verify 1, 2, 4, 5, 6, 9, and 12 are resolved |
| M2-01 | SSH identity, host-key trust, and vsock proxy transport | `agent/m2-transport` | [#17](https://github.com/0xchasercat/firestone/pull/17) | complete | M1 | Exact SSH/vsock transport contracts, key permissions, trust rotation, bounded handshake, cancellation, and binary relay pass without KVM |
| M2-02 | Guest SSH units and cloud-init integration | `agent/m2-guest` | [#18](https://github.com/0xchasercat/firestone/pull/18) | complete | M1 | Typed deterministic guest units activate key-only root/default-user SSH, coexist with native systemd vsock, preserve first-boot hvc0 rescue, and KVM verify 11 and 17 pass |
| M2-03 | Readiness, shell, ssh-config, console, and run CLI | `agent/m2-cli` | [#19](https://github.com/0xchasercat/firestone/pull/19) | complete | M2-01, M2-02 | Shared actions, deterministic output, interactive exec, no-wait behavior, and readiness transitions pass |
| M2-04 | M2 Linux KVM integration | `agent/m2-integration` | [#20](https://github.com/0xchasercat/firestone/pull/20) | complete | M2-01 through M2-03 | E2E 2 and 10 pass; empty-home run reaches a root prompt; verify 11, 13, and 17 close |
| M3-01 | Passt, forward grammar, and tap network plans | `agent/m3-network` | [#23](https://github.com/0xchasercat/firestone/pull/23) | complete | M2 | Exact pinned passt/tap commands, socket/config plans, forward validation, and bounded errors pass without KVM |
| M3-02 | Virtiofsd mount plans and VmConfig mapping | `agent/m3-virtiofs` | [#22](https://github.com/0xchasercat/firestone/pull/22) | complete | M2 | Exact pinned virtiofsd commands, tags, read-only mapping, ownership, and bounded errors pass without KVM |
| M3-03 | User cloud-init parts, SSH keys, and instance identity | `agent/m3-cloudinit` | [#21](https://github.com/0xchasercat/firestone/pull/21) | complete | M2 | Canonical user-first MIME, bounded path inputs, key de-duplication/order, network-config publication, exact byte identities, target cloud-init merge evidence, and deterministic seed goldens pass |
| M3-04 | Network/filesystem sidecars and CLI lifecycle integration | `agent/m3-sidecars` | [#24](https://github.com/0xchasercat/firestone/pull/24) | complete | M3-01 through M3-03 | Exact prepared plans drive VmConfig/results; shim launch, rollback, degradation, VMM-first stop, identity-safe reaping, and Linux recovery cover passt plus ordered virtiofsd sidecars; local stable/1.85 and final-head Ubuntu x86_64 CI pass, while pinned KVM reruns remain M3-05 |
| M3-05 | M3 Linux KVM integration | `agent/m3-integration` | [#25](https://github.com/0xchasercat/firestone/pull/25) | complete | M3-01 through M3-04 | Exact-main `47daf52` KVM acceptance passed E2E 3, 4, and 8 plus verify 7, 8, 14, and 16; `m3.json` SHA-256 is `761766b1ef0a7c34eb2416d5e7cc0cdf7e04c5f6e95be61796e249dcc9580dab`, and verify 10 and 15 remain resolved. |
| M4-01 | Axum REST router, routes, streaming, and error mapping | `agent/m4-api` | [#26](https://github.com/0xchasercat/firestone/pull/26) | complete | M3-04 | Every SPEC route projects shared actions/results, NDJSON framing and limits are exact, handler tests pass with a mocked Dispatcher, and local stable/1.85 plus final-head Ubuntu x86_64 CI are green |
| M4-02 | Serve CLI and HTTP runtime integration | `agent/m4-serve` | [#27](https://github.com/0xchasercat/firestone/pull/27) | complete | M4-01 | Unix-only mode-0600 listener, secure stale/conflict handling, identity-safe cleanup, concurrent requests, bounded signal drain, and real CLI/curl smoke pass; final Ubuntu CI is green |
| M4-03 | M4 REST equivalence and E2E integration | `agent/m4-integration` | [#28](https://github.com/0xchasercat/firestone/pull/28) | complete | M4-01, M4-02 | Exact-main `47daf52` KVM E2E 9 passed real CLI/Unix-serve equivalence, streaming, 204, locking, disconnect, crash-safe stale-socket takeover, restart, and cleanup; `m4.json` SHA-256 is `364223d2be540372ace6cc942a76ff9158baf2965b7b7f03b1cb05756042a322`. |
| M5-01 | CLI progress, timing, output polish, and error hints | `agent/m5-cli-polish` | [#31](https://github.com/0xchasercat/firestone/pull/31) | complete | M4 implementation | Real PTY coverage proves ordered repeated rows, resize-safe updates, timing/rates, color gates, SIGINT cleanup, and exact termios restore; non-TTY, JSON, quiet, and broken-pipe contracts remain deterministic; bare-command help retains clap's readable multiline layout with usage exit 2, while invalid-argument human errors remain sanitized single-line diagnostics; bounded process/VMM diagnostics retain stable kinds, context, hints, and safe log/body previews; local stable/1.85 and final-head Ubuntu CI pass |
| M5-02 | Completions, version, and Linux release artifacts | `agent/m5-release` | [#29](https://github.com/0xchasercat/firestone/pull/29) | complete | M4 implementation | Five deterministic hidden-free completions, shared version metadata, reproducible static x86_64 artifacts/SHA256SUMS, and compile-only aarch64 gate pass; no release or aarch64 runtime claim |
| M5-03 | Catalog gate, fresh-host doctor matrix, and user guide | `agent/m5-docs-catalog` | [#30](https://github.com/0xchasercat/firestone/pull/30) | complete | M4 implementation | Authoritative x86_64 catalog sources, Ubuntu/Fedora/Arch unprivileged doctor matrices, Linux MVP guide, and five-image KVM E2E 11/verify 3 pass |
| M5-04 | Final Linux x86_64 MVP acceptance | `agent/m5-integration` | [#32](https://github.com/0xchasercat/firestone/pull/32), [#44](https://github.com/0xchasercat/firestone/pull/44) | complete | M3-05, M4-03, M5-01 through M5-03 | On Linux 6.8.0-137-generic x86_64 with real KVM, exact clean main `47daf52` passed the sequential M1–M4 and five-image catalog coordinator: E2E 1–11 passed and verify 1–17 resolved. Acceptance SHA-256 is `e29a3fc7d189e29ea7a789783ce585ee4247032aa5be000931964107fd7faff4`; release SHA-256 is `411d26c63e3ea4b0e1aa807165f0f00f08d09ef0fa75726edbb906217bf06505`; exact-main CI `33335967366`, doctor matrix `33335967251`, and release `33335967353` passed. aarch64 runtime, non-Linux authority, and hostile wrappers remain explicitly deferred. |
| M5-05 | Standalone embedded helpers, AppArmor remediation, and guided create UX | `agent/standalone-helpers-create-ux` | [#46](https://github.com/0xchasercat/firestone/pull/46) | ready | accepted Linux x86_64 MVP | Exact commit `285bcf8` produced byte-identical 14,092,624-byte standalone binaries (SHA-256 `b1f1fd72989bfae0287a5ca851cf5ce9db2b99201e4b9ed1b7c90cb0a8c81564`); unprivileged first-use extraction matched passt/qemu-img pins; M3 real-KVM evidence `26bf0f818fae403258281c0a24a57768bdf9218665007fd5c0afb55b3aed4407` passed all network/mount/tap scenarios and cleanup; exact Ubuntu 24.04 AppArmor profile parse/load/runtime and cleanup passed; local gates are green; all 23 PR checks passed for implementation head `6908dc2` |
| M5-06 | Merged catalog discovery and guided image selection | `agent/catalog-command-create-select` | [#47](https://github.com/0xchasercat/firestone/pull/47) | ready | M5-05 | Human and JSON merged-catalog output, `GET /v1/catalog` action projection, mixed per-architecture firmware, user catalog selection, custom URL/path selection, six PTY wizard paths, terminal restoration, 636 Rust tests, fmt, clippy, docs, release-recipe, and M5 harness tests pass |
| M5-07 | Embed pinned Cloud Hypervisor in x86_64 standalone release | `agent/embed-cloud-hypervisor` | [#48](https://github.com/0xchasercat/firestone/pull/48) | in progress | M5-05 | Strict release embedding, secure lazy default-VMM materialization, custom override precedence, doctor repair without VMM download, exact hash/version release smoke, required gates, and final CI pass |
| M5-08 | Interactive create image selector line alignment | `agent/fix-image-selector-layout` | [#50](https://github.com/0xchasercat/firestone/pull/50) | ready | M5-06 | Dialoguer owns temporary raw input while terminal output retains ONLCR; ANSI-stripped real-PTY coverage proves every visible menu row returns to column zero; console keeps its explicit RawTerminal guard |
| M5-09 | Default nano editor fallback | `agent/default-nano-editor` | [#49](https://github.com/0xchasercat/firestone/pull/49) | in progress | M5-06 | `edit` and `create --edit` honor nonempty `VISUAL`, then `EDITOR`, then `nano` through `Cmd`; fake-`nano` process coverage and the required local gates pass |
| M5-10 | Static OpenAPI 3.1 contract and Axum route drift gate | `agent/rest-openapi-spec` | [#52](https://github.com/0xchasercat/firestone/pull/52) | ready | M4-01 | `docs/openapi.json` covers all 18 authored REST operations, Unix transport, JSON/NDJSON/text framing, exact schemas, limits, statuses, nullability, and examples; dependency-free parsing plus behavior-level authored/advertised method comparison detects route drift without a runtime endpoint or behavior change; 17 API tests, 53 documented commands, 6 links, 638 full-suite tests, fmt, and Clippy with warnings denied pass |
| M5-11 | Seamless first-start firmware and passt AppArmor recovery | `agent/seamless-first-start` | [#51](https://github.com/0xchasercat/firestone/pull/51) | ready | M5-07 | Direct first start securely provisions only the selected owned pinned firmware before VmConfig publication; custom firmware is untouched; fatal runtime-mode passt isolation triggers AppArmor repair; verified resolution selects the authorized pinned root copy; 640 Rust tests, format, Clippy, and docs validation pass; Linux/KVM rerun remains outside this macOS worktree |
| M5-12 | Embedded HTMX web UI, loopback listener, and session-token auth | `agent/web-ui` | landed on `main` (`1b879fc`) | complete | M5-10 | SPEC 16.1 TCP form and new 16.5 implemented: loopback-only bind refused before the listener exists, TCP-without-token and token-without-TCP both usage errors before binding, `Host` allowlist ahead of a constant-time token compare, `?token=` bootstrap that strips the query and sets an `HttpOnly; SameSite=Strict` cookie, and cross-origin checks on mutations. The UI renders only shared action results; every lifecycle mutation goes to the documented `/v1` endpoint and renders its NDJSON live, with `POST /ui/machines` the one UI-side write. Strict CSP with no `'unsafe-inline'` or `'unsafe-eval'`; all templates, scripts, styles and fonts embedded; third-party assets pinned by SHA-256 in `web-assets.toml` and enforced by a Rust test. Required local gates pass. Real-browser interaction and Linux/KVM rerun remain open. |
| M5-13 | Release version alignment after the tagged-workflow change | `agent/web-ui` | landed on `main` (`v0.1.4`) | complete | M5-12 | `Cargo.toml`'s workspace version is the only declaration; `identity.release`, the `--version` line and the release artifact name derive from it. The M5-04 harness no longer pins `Cargo.lock` or `versions.env` bytes and instead requires `versions.env` to agree with `Cargo.toml` and `Cargo.lock`; a new `cargo test` guard enforces the same agreement on every change, negative-tested both ways. Fixes the red `store.rs` version assertion and the red doctor-matrix CI job left by `release: v0.1.3`. |

| M6-00 | Green baseline: debian:13 catalog refresh, PTY smoke drain | `agent/m6-00-baseline` | [#54](https://github.com/0xchasercat/firestone/pull/54) | complete | M5 accepted | Doctor-matrix workflow green on main; `cargo test` green on macOS and Linux |
| M6-01 | Metrics action and `GET /v1/machines/{name}/metrics` | `agent/m6-metrics` | [#66](https://github.com/0xchasercat/firestone/pull/66) | complete | M6-00 | SPEC §25 normative; `Action::Metrics` → `MetricsResult` (sampled_at, cpu.cpu_time_ns, memory.rss/allocated/guest_actual, block counters, `net: null` under passt) from `vm.counters` + `vm.info` + `/proc/<vmm_pid>/{stat,status}`; `u64::MAX`-family sentinels never surfaced; `firestone metrics NAME` human table plus `--json`; 409/exit 4 when not running; `docs/openapi.json` route + schemas green under the drift gate; 16 new tests (`/proc` fixtures, sentinel projection, `vm.counters` client, dispatcher conflict paths, renderer table, fake-VMM REST/CLI byte parity); full `cargo fmt --check`, `cargo clippy --all-targets -D warnings`, `cargo test` green |
| M6-02 | Clone with overlay copy | `agent/m6-clone` | [#61](https://github.com/0xchasercat/firestone/pull/61) | complete | M6-00 | SPEC §24 implemented: `Action::Clone` → `CloneResult`, `firestone clone SRC DEST [--fresh-disk]`, `POST /v1/machines/{name}/clone` `{"name", "fresh_disk"?}` streaming NDJSON, and `docs/openapi.json` `cloneMachine`. `ImageStore::copy_overlay` runs the pinned `qemu-img convert -f qcow2 -O qcow2 -o backing_fmt=qcow2 -B <base>` argv through `Cmd` under the store lock with the same partial → mode → fsync → validate → publish-no-replace discipline as `create_overlay`, and is reusable by M6-04. Fake-VMM/fake-qemu coverage proves byte-equal source/destination `firestone.toml`, refusal of a running source before the source lock is taken, self-clone refusal, `--fresh-disk` selecting `create` instead of `convert`, clone-then-boot with a distinct derived MAC and `iid-<dest>-…` instance id, and CLI/REST result-payload equivalence over a real Unix `serve` listener. Local `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` and `cargo test` pass; Linux/KVM rerun and the duplicated guest `/etc/machine-id` (documented in §24.4) remain open |
| M6-03 | Disk grow and live cpu/memory resize | `agent/m6-resize` | [#67](https://github.com/0xchasercat/firestone/pull/67) | complete | M6-00 | SPEC §5.1/§5.2, §7.1/§7.2/§7.4, new §9.5, §10.3, §15.1, §16.2 and three §21 rows implemented. Optional `cpus_max`/`memory_max` (patch, clear, field metadata, template, `cpus_max >= cpus` / `memory_max >= memory` validation) map to `cpus.max_vcpus` and `memory.hotplug_size`; a byte-stability regression test proves a spec without them produces the pre-existing canonical `vm.create` bytes. Disk shrink is refused on `PUT`/`PATCH`/`resize` once an overlay exists; a larger `disk` grows it at start through the new `image.rs` `resize_overlay` (`qemu-img resize`) with guest partition growth left to cloud-init `growpart`. New `Action::Resize` → `ResizeResult{name, applied_live, cpus, memory}`, `firestone resize`, `POST /v1/machines/{name}/resize` NDJSON, and `docs/openapi.json` (19 authored operations). Live path validates headroom against the published `vmconfig.json`, calls the new `vmm_api` `Endpoint::VmResize` (`PUT /api/v1/vm.resize`, 204), and persists the same values; no headroom is a `conflict` hinting `set cpus_max/memory_max and restart`. Firestone's cloud-init part gains the hotplug-cpu udev rule plus `udevadm control --reload`, which changes rendered user-data and therefore instance ids: existing machines reprovision and lose `known_hosts` on their next start; goldens regenerated deliberately. Coverage: validation matrix, headroom refusal, vmconfig byte stability, `vm.resize` request/status contract, udev golden, and a fake-VMM integration test covering the cold patch, the live resize, the headroom refusal on a machine booted without headroom, the shrink refusal, and the overlay grow. `cargo fmt --check`, `cargo clippy --all-targets -D warnings`, and `cargo test` (743 tests) pass on macOS; Linux/KVM rerun remains open |
| M6-04 | Snapshots (cold guaranteed, warm gated on V-5) with shim restore mode | `agent/m6-snapshots` | — | blocked | M6-02, probes V-1/2/5 | Snapshot create/list/restore/rm across CLI/REST; storage under `machines/<name>/snapshots/`; restore is whole-machine rollback with vmconfig byte-equality check in the shim |
| M6-05 | `firestone cp` over the vsock ProxyCommand | `agent/m6-cp` | [#56](https://github.com/0xchasercat/firestone/pull/56) | complete | M6-00 | SPEC 11.9 added plus the 15.1 row, the 5.2 `Action::Cp` entry and a decision-log row. `ssh.rs` now builds one shared option block for `shell`, the readiness probe and the new `scp_command_plan`, so the ProxyCommand, identity, known_hosts, `accept-new` and `LogLevel=ERROR` policy cannot drift. `Action::Cp` classifies operands, requires exactly one `<machine>:<path>` side, refuses a machine that is not running with the `shell` message and hint, and returns the exact argv the CLI execs; no REST route, and the OpenAPI drift test compares routes only, so `docs/openapi.json` is unchanged. Coverage: a 13-row operand-classification table (remote, `./` escape, absolute, bare, uppercase, underscore, empty and IPv6-shaped colons), golden argv tests for `-r`, `%`/space ProxyCommand quoting, the `./` re-escape and BatchMode absence, dispatcher refusals for zero/two remote operands, unknown machine and not-running, a CLI grammar test, and `tests/m6_cp.rs`, a fake-scp-on-PATH smoke over a running fake-VMM machine covering upload, download, escape, scp exit-status passthrough, `--json` refusal and the not-running refusal that never spawns scp. `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` and `cargo test` pass on macOS; no Linux/KVM run is claimed. |
| M6-06 | Pending-forward surfacing | `agent/m6-forward` | [#55](https://github.com/0xchasercat/firestone/pull/55) | complete | M6-00 | SPEC section 12.5 added. `forwards_pending` is on `MachineSummary` and the show payload, in the OpenAPI schemas as a required boolean, and false unless the machine is running. Canonical order-insensitive diff matrix (equal, added, removed, reordered, respelled, unparsable), the not-running gate, the `port forwards apply on restart` warning on running spec writes, the marked `ls` row plus legend, the `show` stderr note, and the REST/OpenAPI projection are covered by unit tests. No KVM run: the pending state needs a live passt machine, so `ls`/`show` against a real running forward set is untested |
| M6-07 | System prune | `agent/m6-prune` | — | blocked | M6-04 | `firestone system prune` + `POST /v1/system/prune`; inert artifacts by default; images/machines tiers gated by flags/force; dry-run list equals act list |
| M6-08 | Cloud-init credentials and inline provisioning | `agent/m6-cloudinit-auth` | [#63](https://github.com/0xchasercat/firestone/pull/63) | complete | M6-00 | SPEC §10.2/§10.3/§10.4 amended plus new normative §10.5, §5.1, §7.1, §7.2, §7.4 and §16.4 rows, two decision-log entries. `cloud_init.user_data_inline` (32 KiB cap, `#cloud-config`/`#!` header, mutually exclusive with `user_data` and byte-identical to it), `ssh_authorized_keys` parsed by the same `ssh-key` path and deduplicated across files and inline entries, `password` rendered as a JSON-quoted `chpasswd` `expire: false` list entry, and `ssh_pwauth` true only when set. Password and inline user-data are redacted from `Debug`, absent from errors, hints, logs and argv (`--password-file` only); `firestone.toml`, its edit candidate, and every seed artifact are published mode 0600 and covered by tests. `ErrorInfo.field` carries the dotted spec path for every §7.2 failure through the REST envelope (JSON and NDJSON) and is ignored by CLI rendering. `docs/openapi.json` updated for `CloudInitSpec`/`Input`/`Patch`, `SpecClear`, the clear/set `allOf` clauses, examples, and `ErrorInfo`. Gate: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (workspace) and `scripts/validate-docs.py` pass on macOS; no Linux/KVM run in this worktree |
| M6-10 | OCI SPEC sections and pinned direct-boot kernel entries | `agent/m6-oci-spec-pins` | [#58](https://github.com/0xchasercat/firestone/pull/58) | complete | probes V-6 | SPEC gains normative §8.5 (OCI images), §9.5 (direct kernel boot), §10.5 (`firestone-init`), §11.8 (OCI machines), the `images.insecure_registries` global key, three §17.2 binary rows, verify 21–25, and eleven §21 decision rows. `scripts/pin-deps.sh` regenerates `deps.toml` with `[dependency.cloud-hypervisor-kernel]` at `ch-release-v6.16.9-20260508`; `verify --arch all` passed and confirmed x86_64 `bzImage-x86_64` SHA-256 `58088758f601a04ef85b09cf23db5530d51edc039ed47afbf2264c5b762cb568` (8,385,024 bytes) and aarch64 `Image-arm64` SHA-256 `69d1b1235381ec50f1b45cf771a7dff4a9013d452833ab34682d6283e2114010`. The new `deps.toml` SHA-256 `20e8023d5c8d829ecdf688019ada96a12df74e5eb0f2d8b24a7a8f83eb55e202` is updated in `build/firestone/versions.env` and `scripts/m5-linux-mvp-e2e.py`. `DependencyManifest::direct_boot_kernel` resolves the per-architecture mode-0644 artifact with unit coverage; no consumer yet (M6-16 wires it) and no REST route changed |
| M6-11 | Static mkfs.ext4 helper (e2fsprogs tar-input) | `agent/m6-mkfs-helper` | [#57](https://github.com/0xchasercat/firestone/pull/57) | complete | probe V-7 | `build/helpers` builds e2fsprogs 1.47.3 `mkfs.ext4` static against libarchive 3.8.3 from digest-pinned, hash-locked inputs with a twice-build byte-identity check; container smoke proves tar input keeps uid 12345 ownership, `0741` mode, symlink, hard link and `01:03` device node; GPL corresponding source and license texts ship in the bundle; `build/helpers/tests/test-build-scripts.sh` and `shellcheck` pass; publish-then-pin steps recorded in `docs/verification/mkfs-ext4-helper.md`; `deps.toml` untouched to avoid the M6-10 collision |
| M6-12 | OCI reference parsing and resolution | `agent/m6-oci-ref` | [#59](https://github.com/0xchasercat/firestone/pull/59) | complete | M6-00 | SPEC §8.5 implemented in a new `firestone-core::oci` module: `oci://`/`docker://` prefixes plus the first-component `.`/`:`/`localhost` heuristic, `docker.io` + `library/` + `latest` normalization, `sha256` digest refs, and a `Display` that re-parses to itself. The branch sits between the HTTPS check and the catalog in `ImageStore::resolve`, `resolve_persisted`, and spec validation, with a heuristic-only parse failure falling through unchanged. Regression-tested: `ubuntu:24.04`, bare `nginx` (still a catalog error, hint now names `docker://nginx`), `owner/app`, `./file.qcow2`, absolute paths, and HTTPS URLs all resolve exactly as before. `--sha256` with an OCI ref is a usage error pointing at `repo@sha256:…`; `pull_locked` returns a dependency-kind placeholder until M6-15. `[images].insecure_registries` accepts bare `host`/`host:port` entries and rejects schemes, paths, credentials, and bad ports. `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` all pass (486 core unit tests plus the CLI, API, and shim suites); two fake-VMM timing tests flake under heavy machine load on both this branch and unmodified `main`. No REST route changed, so `docs/openapi.json` is untouched. Linux/KVM rerun remains open in this macOS worktree. |
| M6-13 | Registry V2 client | `agent/m6-oci-registry` | — | blocked | M6-12 | Anonymous Bearer + basic auth from docker config; index platform selection; digest-verified blob streaming; fake-registry tests |
| M6-14 | OCI layer merge to canonical tar | `agent/m6-oci-layers` | [#65](https://github.com/0xchasercat/firestone/pull/65) | complete | M6-00 | `crates/firestone-core/src/oci/layers.rs` merges gzip layer tars in two passes: whiteout, opaque, file-over-dir and dir-over-file replacement, hardlink promotion and re-pointing for shadowed targets, un-followed symlinks, block-for-block preservation of mode/owner/mtime/PAX-xattr/GNU long names and device entries, absolute/`..`/root-escape rejection, and 8 GiB and 1M entry caps as `dependency` errors. `OciImageConfig` and the media-type classifier are exported for M6-15; injection writes `/etc/firestone-oci` and 0755 root `/sbin/firestone-init`. 22 table-driven unit tests including byte-identical repeat output; `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` and `cargo test` pass (the pre-existing macOS `m3_sidecars_cli_lifecycle_uses_exact_plans_and_recovers_degradation` flake reproduces on clean `main` under a parallel full-suite run and passes in isolation). No REST route added, so `docs/openapi.json` is unchanged; mkfs and pull wiring stay with M6-11/M6-15 |
| M6-15 | OCI pull pipeline and sidecar v2 | `agent/m6-oci-pull` | — | blocked | M6-11, M6-13, M6-14 | layers→mkfs→qcow2→publish under the existing store lock and progress events; sidecar `kind`/`oci` with v1 read-compat; manifest-digest caching |
| M6-16 | Direct kernel boot | `agent/m6-kernel-boot` | — | blocked | M6-10 | `PayloadConfig.cmdline`, invariant updates, lazy kernel install before vmconfig publication |
| M6-17 | firestone-init PID 1 and OCI boot integration | `agent/m6-init` | — | blocked | M6-15, M6-16 | New workspace crate; config-disk contract; mounts, DHCP (unless kernel ip=dhcp verified), ext4 online grow, entrypoint supervision, poweroff-on-exit; embedded like helpers |
| M6-20 | ANSI console logs | `agent/m6-ansi-logs` | [#60](https://github.com/0xchasercat/firestone/pull/60) | complete | M6-00 | SPEC §11.7 log-sanitization block and §16.5 log-colour paragraph added. `TerminalSanitizer` in `render.rs` allowlists `ESC [ [0-9;]{0,16} m` and swallows every other sequence as one U+FFFD; it is stateful across chunks and flushes a dangling sequence on `finish`. Wired into the logs surface only: one sanitizer per follow stream in `api.rs` `logs_response`/`text_stream_response`, and `sanitize_terminal_output` for the aggregated api+ui path; every other `write_safe_output` caller is untouched. `app.js` gains `parseSgr`, `foldCarriageReturns` and a line-oriented renderer shared by the first screenful and the follow append; `app.css` gains `--fs-ansi-0..15` in both themes with `fs-ansi-*` classes, no inline styles. Evidence: 3 Rust sanitizer tests (SGR passthrough, split-boundary, OSC 52, OSC-BEL title, DCS/APC, raw C1, overlong CSI, dangling ESC at EOF, CR/LF preservation) plus a router test asserting verbatim `ESC[32mok ESC[0m` and a neutralized OSC in the fragment; `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` and `cargo test` green on macOS. Browser rendering and Linux/KVM rerun not exercised here. |
| M6-21 | WebSocket console and shell transport | `agent/m6-ws-console` | [#62](https://github.com/0xchasercat/firestone/pull/62) | complete | M6-00 | SPEC §16.1 gate step 4, §16.2 rows, §16.3 WebSocket framing subsection, §16.5 terminals note, §21 row; axum `ws` + `serve_connection(...).with_upgrades()`; `/v1/machines/{name}/console/ws` bridges the broker with the acknowledgement read pre-upgrade (BUSY→409 naming `firestone console <name>`, not-running→standard error) and `/v1/machines/{name}/shell/ws` runs the shared ssh argv on a rustix pseudo-terminal with `TIOCSWINSZ` resize and process-group kill on close; upgrade requests additionally pass the same-origin rule. Gate green (`fmt --check`, `clippy --all-targets -D warnings`, `cargo test`) on macOS and on Linux, where the `cfg(linux)` shim signal paths also translate `ProcessSignal::WindowChange` and clippy's `result_large_err` sees a larger tungstenite error: auth gate table, PTY open/resize/nonblocking, control-frame parsing and resize plumbing, per-route EOF close reason (`machine stopped` on the console, `session ended` on the shell), and loopback WebSocket round-trip, EOF close frame, 409 and not-running integration tests against `serve_until_shutdown`. No KVM verification; browser client lands with M6-22 |
| M6-22 | ghostty-web terminal page | `agent/m6-terminal` | — | blocked | M6-21 | Pinned wasm+js assets; `/machines/{name}/terminal` page with Console/Shell tabs; `wasm-unsafe-eval` CSP scoped to that page only |
| M6-23 | Create-form UX overhaul | `agent/m6-create-ux` | [#64](https://github.com/0xchasercat/firestone/pull/64) | complete | M6-00 | Image picker with cached badges and in-dialog pull, numeric size inputs with GiB/MiB units, tap device and mac fields, repeatable forward and mount rows, shared field macros. Done: SPEC §16.5 spec-fields amendments plus decision-log row; `_spec_fields.html` macros and the read-only `GET /ui/machines/new/images` fragment; `CreateForm` gains `tap`/`mac`/`mounts` with per-row mount errors; detail spec gains the Mounts group. Evidence: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` green except the pre-existing `shim::start_preparation_orders_image_overlay_seed_vmconfig_and_plan`, which fails identically on clean `c40cfc1` on this non-KVM macOS host. Limits: no Linux/KVM or real-browser pass; no REST route added, so `docs/openapi.json` is unchanged |
| M6-24 | Machine edit surface | `agent/m6-edit` | [#73](https://github.com/0xchasercat/firestone/pull/73) | complete | M6-23, M6-03, M6-08 | SPEC §16.5 gains the edit-surface, field-error and applies-when/drift blocks plus a §21 row. `GET /ui/machines/{name}/edit` renders `templates/ui/edit.html` from the wave-1 `_spec_fields.html` macros, prefilled through the same `CreateForm` projection the create dialog uses and carrying it again as JSON in `data-fs-original`; the Edit button sits in `_detail_head.html` for a machine in any state. All writes stay on `/v1`: `patchFromForm` in app.js diffs the form against the original into a sparse `MachineSpecPatch` (emptied optional leaves become `clear` entries), a running machine's cpus/memory go to `POST /v1/machines/{name}/resize` through the existing NDJSON drawer, and a list removal is sent clear-first as two patches because an action patch appends and a layer may not clear and set one leaf. Rejections render against `ErrorInfo.field` mapped to the form's field names, everything unattributable lands in a dialog banner. `MachineDetail` gains `forwards_pending`, `drift` and `drift_summary`, comparing only what `state.json` records (image, mac, forwards); the rest is a client-side pill raised on a successful edit-while-running and dropped when the machine is next seen not running. Also repairs a wave-1 merge defect: the M6-23 create-dialog section had been spliced into the middle of `applySgr`, leaving three braces unclosed, so `app.js` did not parse and **no** UI JavaScript ran on `main`; the closing braces are restored and a new `the_embedded_runtime_script_closes_every_block` test guards that class of damage. Evidence: 6 new router tests (prefill incl. mount/forward rows, original-vs-control equality, running and stopped badge variants, Edit button in three states, drift pill only while running), the brace guard, `node --check crates/firestone/assets/ui/app.js`, and the existing no-second-mutation-surface test extended to cover the edit fragment; `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` and `cargo test` green. Limits: no REST route added, so `docs/openapi.json` is unchanged; `patchFromForm` and the drift pill are exercised in a browser only by hand; no Linux/KVM pass; cloud-init fields stay with M6-27 |
| M6-25 | Metrics UI | `agent/m6-metrics-ui` | — | blocked | M6-01 | Poll ring buffer, hand-drawn SVG sparklines, detail tiles and overview utilization; no chart library |
| M6-26 | Snapshot/clone/prune/images UI surfaces | `agent/m6-ui-features` | — | blocked | M6-02, M6-04, M6-07 | Snapshots tab, clone dialog, cached-images management with delete/prune, system prune with dry-run preview, forward chips with open-in-browser links |
| M6-27 | Cloud-init and credentials UI | `agent/m6-ui-cloudinit` | — | blocked | M6-08, M6-23 | Provisioning section: user-data textarea, keys, password toggle in create and edit |
| M6-28 | UI polish pass | `agent/m6-ui-polish` | — | blocked | M6-20..M6-27 | Empty states, palette actions, toast and refresh audit, SPEC §16.5 alignment |
| M6-30 | M6 KVM e2e harnesses | `agent/m6-e2e` | — | blocked | features | `scripts/m6-kvm-e2e.py` and `scripts/m6-oci-kvm-e2e.py` with pytest coverage |
| M6-31 | End-to-end documentation rewrite | `agent/m6-docs` | — | blocked | all features | `docs/user-guide.md` replaced end-to-end and green under `validate-docs.py`; README refreshed |
| M6-32 | M6 acceptance and v0.2.0 release | orchestrator | — | blocked | M6-30, M6-31 | Full e2e plus regression green on bare-metal `w`; real-browser UI pass; release workflow tags v0.2.0 |

## Merge order

1. M0-00 foundation
2. M0-03a and M0-05a when their evidence is complete
3. M0-01, M0-02, M0-03b, M0-05b, and M0-05c
4. M0-04
5. M0-06 integration
6. M1-01, M1-02, and M1-03 in parallel
7. M1-04 shim integration
8. M1-05 lifecycle projection
9. M1-06 KVM integration

The orchestrator reviews each pull request for spec alignment, public contract drift, unsafe filesystem or process behavior, test quality, and dependency direction. A passing branch gate is necessary but does not replace review.

## Infrastructure state

| Resource | State | Next action |
|---|---|---|
| GitHub remote | `git@github.com:0xchasercat/firestone.git`; `main` baseline pushed | All implementation enters through reviewed pull requests |
| Local macOS toolchain | Rust 1.97.1, Cargo 1.97.0 | Use for fast unit feedback; Linux behavior requires remote validation |
| Azure Linux host | Unreachable on TCP/22 during final delivery; prior M1/M2 KVM evidence remains recorded | Restore only if future nested-KVM validation needs this host |
| Bare-metal host `w` | Final Linux x86_64 MVP acceptance passed; KVM read/write; temporary user, sudoers, TAP, policy changes, images, and test roots cleaned | Use a fresh isolated setup for future KVM release gates |

## Completed work

- M0-00 merged in PR [#1](https://github.com/0xchasercat/firestone/pull/1). It established the Rust workspace, closed shared action and event contracts, typed errors, the dispatcher seam, and the required CI gate. The merge passed local, GitHub-hosted Ubuntu, and Azure Linux verification.
- M0-03a merged in PR [#2](https://github.com/0xchasercat/firestone/pull/2). It added ten source-validated Ubuntu, Debian, and Fedora catalog entries plus a bounded HTTPS validator. All entries remain behind the explicit boot-to-SSH release gate.
- M0-05a merged in PR [#3](https://github.com/0xchasercat/firestone/pull/3). It pinned Cloud Hypervisor v53.0, RHF 0.5.0, the VMM-tested edk2 build, and virtiofsd 1.14.0 source. It also resolved the source-level portions of verify items 1, 2, and 12 against exact versions.
- M0-02 merged in PR [#4](https://github.com/0xchasercat/firestone/pull/4). It added durable atomic state writes, per-machine locking, verified shim identity, and exhaustive liveness reconciliation. Linux subprocess contention and `/proc` checks passed on the Azure host.
- M0-03b merged in PR [#5](https://github.com/0xchasercat/firestone/pull/5). It added deterministic built-in and override catalog parsing, strict HTTPS/source validation, and default, alias, version, and architecture resolution.
- M0-01 merged in PR [#6](https://github.com/0xchasercat/firestone/pull/6). It added the shared machine spec and patch model, typed clears, deterministic layering and persistence, startup path resolution, strict validation, scalar and port-forward types, schema/metadata drift gates, and secure runtime-directory ancestry checks.
- The reproducible virtiofsd recipe merged in PR [#7](https://github.com/0xchasercat/firestone/pull/7). Both required musl targets reproduce byte-for-byte in GitHub and on bare metal, and deterministic tar packages preserve modes and provenance.
- Host diagnostics merged in PR [#8](https://github.com/0xchasercat/firestone/pull/8). It added the deterministic 13-check doctor report, safe unprivileged repairs, process-group timeouts, and live state reconciliation.
- Public virtiofsd pins merged in PR [#10](https://github.com/0xchasercat/firestone/pull/10). Anonymous versioned assets for x86_64 and aarch64 match the immutable SHA-256 values in `deps.toml` and the reproducible source recipe.
- The M0 CLI merged in PR [#9](https://github.com/0xchasercat/firestone/pull/9). It projects create, list, show, edit, and doctor through shared contracts; emits deterministic human and NDJSON output; preserves crash-safe publications; validates owned data paths; and retains live supervision state.
- M0 integration completed on main at merge `de97ad5`. The 340-test gate, public dependency verification, create/list smoke, interrupted-publication recovery, and unsafe-path refusal passed on macOS, GitHub CI, and the Azure Linux host.

- M1 image storage merged in PR [#13](https://github.com/0xchasercat/firestone/pull/13). It added bounded immutable qcow2 storage, strict sidecars, cache generations, crash recovery, descriptor-relative path creation, architecture-specific firmware, and overlay validation.
- M1 seed and VmConfig mapping merged in PR [#12](https://github.com/0xchasercat/firestone/pull/12). It added deterministic CIDATA publication, Firestone cloud-init, typed canonical Cloud Hypervisor v53 JSON, and invariant-preserving merge patches.
- M1 VMM transport merged in PR [#11](https://github.com/0xchasercat/firestone/pull/11). It added the bounded Unix-socket HTTP client, exact endpoint/status contracts, and status-only liveness probing. The integrated foundation has 431 passing tests on macOS and GitHub Linux CI.
## Known risks

- Runtime portions of the SPEC section 20 checks remain open unless the decision log explicitly records exact-version evidence.
- The Azure VM has only 29 GB total storage. Image-matrix tests must prune artifacts between runs.
- aarch64 runtime testing needs a separate native host or CI runner. Cross-compilation alone cannot close M5 acceptance.
